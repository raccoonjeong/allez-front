export interface Match {
  matchId: number;
  teamName: string;
  opponentTeamName: string;
  seasonYear: number;
  matchDate: string;
  homeAway: string;
  winDrawLoss: string;
  gain: number;
  loss: number;
}